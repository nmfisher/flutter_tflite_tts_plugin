name: flutter_tflite_tts_plugin_example
description: Demonstrates how to use the flutter_tflite_tts_plugin plugin.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.12.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers:
  flutter_tflite_tts_plugin:
    path: ../
  flutter_tflite:
    path: ../../flutter_tflite
  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/mb_melgan.tflite
    - assets/fastspeech2_quan.tflite
    - assets/tts_config.json
                                     
